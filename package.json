{
  "name": "js-training",
  "version": "1.1.0",
  "description": "JS training tasks",
  "scripts": {
    "lint": "eslint ./**/*.js --fix",
    "test": "./node_modules/.bin/mocha",
    "test-debug": "./node_modules/.bin/mocha --debug-brk"
  },
  "author": "IT Shark Pro",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.4.0",
    "husky": "^1.0.0",
    "mocha": "^5.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git@git.github.com:w-trpo/lab1_Osipuk_part2.git"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run lint && npm test"
    }
  }
}